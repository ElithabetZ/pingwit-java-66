spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/carplay
    username: postgres
    password: root

  jpa:
    properties:
      hibernate:
        default_schema: public

    show-sql: true